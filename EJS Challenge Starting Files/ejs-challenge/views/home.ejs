<%- include("partials/header"); %> 

<h1>Home</h1>
<p><%= homeContent %> </p>

<% posts.forEach(function (post) { %>
    <% var route = "/posts/" + post._id; %> 
    <% console.log (route) %> 
    <h1><%= post.name %> </h1>
    <p><%= post.body.substring(0, 100) %> ... <a href="<%= route %>" >Read More</a> </p>
<% }) %> 

<%- include("partials/footer"); %> 